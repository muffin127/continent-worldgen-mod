/**
 * Deterministic seed-derivation utilities. Converts the world seed into
 * independent sub-seeds for continent shape, climate, regions, rivers and
 * structures, so that each subsystem gets reproducible but decorrelated
 * randomness.
 */
package com.muffin120706.worldweaver.common.worldmodel.seed;
