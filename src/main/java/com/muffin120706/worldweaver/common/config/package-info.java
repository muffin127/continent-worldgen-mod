/**
 * Loader-agnostic configuration model (world size, required biomes,
 * structure limits) parsed independently of any specific mod-loader config
 * API.
 */
package com.muffin120706.worldweaver.common.config;
